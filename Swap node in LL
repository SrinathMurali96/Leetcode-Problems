#Recursive approach

def swap(self,head):
  if not head or not head.next:
      return head
  first = head
  second = head.next
  first.next = self.swap(second.next)
  second.next = first
  return second
